# Session 1-2 findings, re-checked on the full dataset

_Generated by `scripts/verify_findings.py` on 2026-09-22. 10 findings, each re-computed in code against all 10,480 images / 5,240 lesions — not a sample._

| finding | checked on full dataset | evidence | consequence for pipeline | shortcut risk |
|---|---|---|---|---|
| Every lesion has exactly 2 images (one dermoscopic, one clinical close-up), so images are not independent samples. | verified | 5,240/5,240 lesions have exactly 2 images (min 2, max 2); 5,240/5,240 have 2 distinct image_type values; 10,480 image rows, 10,480 unique isic_id. In the committed splits the lesion overlap is 0/0/0 (train-val / train-test / val-test) and every lesion keeps both of its images: True. | Split on lesion_id with a grouped splitter (splits.split_lesions), never on image rows. A row-level split would put the same piece of skin on both sides and inflate every score. | lesion_id is in config.LEAKY_COLUMNS: it is the grouping key, never a model input — as a feature it would let the model memorise individual lesions. |
| The 3-class target is dominated by one class: ~69% of lesions are Malignant, so plain accuracy is not a meaningful metric. | verified | Benign 1,483 (28.3%); Indeterminate 123 (2.3%); Malignant 3,634 (69.4%) of 5,240 lesions. A constant 'Malignant' predictor scores accuracy 69.35% but balanced accuracy 33.33% and macro-F1 27.30%, with 0% recall on Benign and Indeterminate. | Headline metrics are balanced accuracy, macro-F1, per-class recall and malignant sensitivity. Accuracy may be reported only next to the constant-predictor baseline above, so the reader can see what it is worth. | No feature involved — but the prior itself is the shortcut a model will take if the loss is unweighted. |
| The 11-class stretch target has a long tail — imbalance around 280:1. | verified | Full dataset BCC 2,522 vs MAL_OTH 9 = 280.2:1. On the TRAIN split the ratio is 252.3:1 (BCC 1,766 vs MAL_OTH 7), which is the number the loss actually sees. MAL_OTH lands 7/1/1 in train/val/test. | Class weights (balanced and inverse-sqrt, computed on TRAIN only, artifacts/class_weights.json) plus an optional WeightedRandomSampler. No class is merged away: a MAL_OTH test set of 1 lesion is reported as a count, never as a percentage. | None. This is a property of the labels, not a feature. |
| Dermoscopic and clinical close-up images have different pixel statistics — two modalities, not two samples of one. | verified | Channel means over 5,240 dermoscopic vs 5,240 clinical images at 224px: R 0.670/0.688, G 0.544/0.509, B 0.537/0.415; mean brightness 0.584 vs 0.537 (-0.046). The largest gap is B at -0.122. NOT a label shortcut: image_type x diagnosis_1 is flat — dermoscopic 28.3/2.3/69.4 vs clinical 28.3/2.3/69.4 % Benign/Indeterminate/Malignant (max gap 0.00 pp). File size scores malignancy at ROC-AUC 0.464 (dermoscopic) and 0.511 (clinical) — Malignant vs rest, i.e. chance — while separating the modalities strongly (median 26.0 vs 38.8 KB). | Normalisation statistics are computed over the mixed training set, not per modality, because Milestone 2 feeds both views to one backbone. image_type stays in the split files so results can be reported per modality, and evaluation aggregates the two views per lesion (datasets.aggregate_predictions). | Low, and measured. image_type predicts nothing about the label, so it is safe as a reporting slice or a conditioning input; file size is chance-level on malignancy so no compression shortcut exists. |
| AKIEC straddles the coarse target: the same 11-class code appears as both Indeterminate and Malignant, so diagnosis_1 is not derivable from the 11-class code. | verified | Ambiguous codes: ['AKIEC']. AKIEC = 303 lesions, 123 Indeterminate + 180 Malignant; the other 10 codes each map to exactly one diagnosis_1 value. All 123 Indeterminate lesions are AKIEC ({'AKIEC': 123}). | Two label columns are stored side by side in the split files (diagnosis_1 and dx) and two label maps in artifacts/label_map.json. Indeterminate is kept as a real third class, not merged into Malignant: the pathologist declining to commit is clinical information. | None, but it blocks a tempting shortcut in the other direction — deriving the 3-class label from an 11-class prediction by lookup would silently mislabel every AKIEC lesion. |
| Metadata is heavily incomplete: anatom_site_general missing ~37%, melanocytic missing ~77%, and melanocytic leaks the label. | nuanced | Image level: anatom_site_general missing 37.33%, melanocytic missing 77.18% (True on 2,392 images, False on 0, absent on the rest — so its mere presence already says 'melanocytic lesion', which makes it a coarse label in disguise). The missingness is NOT at random: NV is 10.14% of lesions with a recorded site vs 21.11% of lesions without (+10.97 pp), and the Malignant share moves from 71.8% to 65.2%. | anatom_site_general becomes an explicit 'unknown' category — never mode-imputed, because the absence is itself weakly predictive and imputing would both destroy that signal and invent a site for 37% of the rows. melanocytic stays in config.LEAKY_COLUMNS and is never loaded as a feature. age_approx (0.4% missing) is imputed with the TRAIN median only. | HIGH for melanocytic — it is derived from the diagnosis, so it is banned as an input. MODERATE for 'unknown site': it is a real, usable weak feature, but it encodes which contributor uploaded the lesion, so any metadata model must be reported without it as well. |
| Images vary in resolution, so the pipeline needs a resize step to make batching possible. | refuted | All 10,480 files were opened: 10,480 readable, 0 missing, 0 unreadable. Width min/median/max = 600/600/600, height 450/450/450. n_distinct_resolutions = 1 (600x450, 100.0% of readable images); 100.0% are at least 224px and 100.0% at least 256px on both sides. The dataset is dimensionally uniform; the Session 1 claim came from a 400-image sample and does not survive the full check. | A resize is still applied, but for a different reason: not to reconcile ragged inputs, but purely to match the backbone's expected input. Because every image is 600x450, the resize is a pure downscale with no upsampled special cases and no padding branch — see docs/preprocessing_decisions.md. | None — and that is itself the useful result. Varying resolution would have been a per-contributor fingerprint the model could key on; a single uniform resolution removes that possibility. |
| MILK10k is a biopsy-referred dataset: lesions are here because a clinician already suspected something. | verified | 10,032/10,480 images (95.7%) are confirmed by histopathology; concomitant_biopsy flags the identical 10,032 images (mask equality checked: True); the remaining 448 are single-contributor clinical assessment. Malignant prevalence is 69.4% of lesions — a referral-clinic prior, not a population one. | Every performance claim names the population: 'on biopsied, referral-clinic lesions'. No threshold is tuned as if the model were a screening tool, and the test split is reported per class rather than as a single headline number. | Indirect: the referral filter is baked into the images themselves, so it cannot be removed — only declared. It is the reason the acquisition columns below leak. |
| Acquisition metadata could encode the label: image_manipulation (altered vs instrument only) is a candidate shortcut. | verified | Row percentages over 335 altered vs 10,145 instrument-only images: altered = 39.7% Benign / 12.8% Indeterminate / 47.5% Malignant, instrument only = 27.9 / 2.0 / 70.1. A real association (22.6 pp on Malignant), on only 3.2% of images. | Not used as a model input. It is kept in the split files as a reporting slice so a per-group metric can show whether the 335 altered images behave differently at test time. | MODERATE. The association is real but it describes the photographer, not the lesion; as an input it would let the model score points on 3.2% of images for free. |
| diagnosis_confirm_type records how the diagnosis was confirmed and is available at training time — is it usable as a feature? | verified | 72.3% of the 10,032 histopathology-confirmed images are Malignant vs 2.2% of the 448 clinical-assessment images — a 70.1 pp gap, the largest of any metadata column. The column is only knowable after the biopsy that produced the label. | Listed in config.LEAKY_COLUMNS and excluded by construction: the dataloaders read image pixels plus the label column only. quality.leaky_columns_table() prints the ban list with a reason per column, so the exclusion is auditable rather than asserted. | SEVERE — effectively a label. Never a model input, in any milestone. |

## Verdict counts

- **verified**: 8
- **nuanced**: 1
- **refuted**: 1

A refuted finding is a result. Finding 7 (images vary in resolution) came from a 400-image sample in Session 1; opening all 10,480 files shows a single resolution, which is why the resize decision in `docs/preprocessing_decisions.md` has no special cases.
